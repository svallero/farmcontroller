
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: farms.farmcontroller.toinfn.it
spec:
  group: farmcontroller.toinfn.it
  names:
    kind: Farm
    listKind: FarmList
    plural: farms
    singular: farm
  scope: ""
  subresources:
    scale:
      specReplicasPath: .spec.replicas
      statusReplicasPath: .status.replicas
  validation:
    openAPIV3Schema:
      description: Farm is the Schema for the farms API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: FarmSpec defines the desired state of Farm
          properties:
            labelKey:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file'
              type: string
            labelValue:
              type: string
            minExecutors:
              format: int32
              type: integer
            replicas:
              format: int32
              type: integer
          required:
          - labelKey
          - labelValue
          - minExecutors
          - replicas
          type: object
        status:
          description: FarmStatus defines the observed state of Farm
          properties:
            allExecutors:
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              format: int32
              type: integer
            errorExecutors:
              format: int32
              type: integer
            overquota:
              format: int32
              type: integer
            pendingExecutors:
              format: int32
              type: integer
            replicas:
              format: int32
              type: integer
            runningExecutors:
              format: int32
              type: integer
          required:
          - allExecutors
          - errorExecutors
          - overquota
          - pendingExecutors
          - replicas
          - runningExecutors
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
