
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: farmmanager.farmcontroller.toinfn.it
spec:
  group: farmcontroller.toinfn.it
  names:
    kind: FarmManager
    listKind: FarmManagerList
    plural: farmmanager
    singular: farmmanager
  scope: Cluster
  validation:
    openAPIV3Schema:
      description: FarmManager is the Schema for the farmmanagers API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: FarmManagerSpec defines the desired state of FarmManager
          properties:
            labelKey:
              description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                Important: Run "make" to regenerate code after modifying this file'
              type: string
            labelValue:
              type: string
          required:
          - labelKey
          - labelValue
          type: object
        status:
          description: FarmManagerStatus defines the observed state of FarmManager
          properties:
            overquota:
              additionalProperties:
                format: int32
                type: integer
              description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                of cluster Important: Run "make" to regenerate code after modifying
                this file'
              type: object
            pending:
              additionalProperties:
                format: int32
                type: integer
              type: object
            tobescaleddown:
              additionalProperties:
                format: int32
                type: integer
              type: object
            underquota:
              additionalProperties:
                format: int32
                type: integer
              type: object
          required:
          - overquota
          - pending
          - tobescaleddown
          - underquota
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
